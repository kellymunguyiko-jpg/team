rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open rules so admin add/edit/delete works for demo.
    // Lock these down later: allow write only if request.auth != null
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
